feat: identity matching margin test and tracking adjustments

Improvements in Matching Logic:
- Updated `face_database.py` `match` method to retrieve `top_k` embedding candidates.
- Implemented a margin test to reject matches where the gap between the top-1 and top-2 identity scores is less than `match_margin`. This prevents false accepts on near-ties.
- Read `match_margin` and `match_top_k` from `thresholds.yaml` in `main.py` and pass them to the matching logic.

Quality Scoring and Tracking Adjustments:
- Factored face width into `quality_score` in `main.py` using `size_factor` to prioritize larger faces during temporal consensus.
- Increased `tracking.max_age` to 15 frames in `thresholds.yaml` to allow tracks to coast longer without a detection.
